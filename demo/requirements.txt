# Auto-demo recorder dependencies (separate from the app's runtime deps).
# After installing, fetch the browser binary once:
#   python -m playwright install chromium
playwright>=1.44.0
# Bundled ffmpeg binary — used by record_narrated.py to mux the TTS voiceover
# into the demo video (no system ffmpeg needed).
imageio-ffmpeg>=0.4.9
edge-tts>=6.1.0
